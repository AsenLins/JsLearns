define(function(){
  var watchDom=function(){
      var domList={};
  };
  var obj={
    width:100,
    height:100
  }

  Object.defineProperties(obj,"width",{
    set:function(key,value){
      return value;
    }
  });


});
